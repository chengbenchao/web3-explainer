# AGENTS.md - 智能体入口地图
> 保持简短，约 100 行。这是智能体的入口，不是百科全书。

## 🎯 项目使命
[一句话描述项目的核心目标]

## 🗺️ 代码仓库导航

### 核心文档位置
- **架构总览** → `docs/architecture/README.md`
- **工程原则** → `docs/principles/`
- **代码模式** → `docs/patterns/`
- **执行计划** → `docs/execution-plans/`
- **风格指南** → `docs/style-guide/`
- **品味不变量** → `docs/taste/`

### 代码结构
```
src/
├── domain/       # 业务域逻辑
├── services/     # 服务层
├── repos/        # 数据访问层
├── config/       # 配置
└── types/        # 类型定义
```

## 🔧 架构约束
1. **严格分层**：只能向前依赖 `Types → Config → Repo → Service → Runtime → UI`
2. **边界解析**：所有外部数据必须在边界解析，不允许在内部验证
3. **依赖注入**：所有依赖必须显式注入，不允许隐式单例

## 📋 工作流程
1. 找到相关文档和上下文
2. 制定执行计划（复杂任务）
3. 编写代码和测试
4. 自我审查更改
5. 打开 PR 并等待反馈

## ⚠️ 黄金原则
1. 优先使用共享实用程序包，不要重复造轮子
2. 不要 YOLO 式探测数据 — 总是验证边界或使用类型化 SDK
3. [添加你的项目特定原则]

---
*记住：这是地图，不是说明书。深入研究请查看链接的文档。*
